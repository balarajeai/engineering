# Security Review

## Review ID

## Component

## Change

Task ID:
PR:
Risk level: `LOW` / `MEDIUM` / `HIGH` / `CRITICAL`

## Assets

## Trust boundaries

## Authentication

`PASS` / `FAIL` / `N/A` —

## Authorization

`PASS` / `FAIL` / `N/A` —

## Tenant isolation

`PASS` / `FAIL` / `N/A` —

## Injection

`PASS` / `FAIL` / `N/A` —

## Secrets

`PASS` / `FAIL` / `N/A` —

## Logging

`PASS` / `FAIL` / `N/A` —

## Data exposure

`PASS` / `FAIL` / `N/A` —

## Dependencies

`PASS` / `FAIL` / `N/A` —

## Network impact

`PASS` / `FAIL` / `N/A` —

## Findings

### Finding

- ID:
- Title:
- Severity: `LOW` / `MEDIUM` / `HIGH` / `CRITICAL`
- Affected component:
- Attack path:
- Reproduction:
- Impact:
- Recommended remediation:
- Verification method:
- Status: `OPEN` / `FIXED` / `ACCEPTED`

Repeat as needed. Independent verification is required after a material fix. The fixer MUST NOT be the only approver of that finding.

## Remediation

Summary of required fixes and owners.

## Verification

What was actually re-tested after remediation.

## Human exception

If unresolved HIGH or CRITICAL residual risk is accepted:

- Accepting human:
- Date:
- Reason:
- Expiry or follow-up:

Agents MUST NOT invent this section.

## Final result

`PASS` / `FAIL` / `PASS WITH NON-BLOCKING FINDINGS`

Release blocked? Yes / No

Unresolved CRITICAL or HIGH findings MUST block release unless explicitly accepted above.
